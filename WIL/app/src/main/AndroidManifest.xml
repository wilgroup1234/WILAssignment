<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.a17001922.wil_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="LOGIN"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".homeScreen.addCustomGoalScreen"></activity>
        <activity android:name=".homeScreen.addNormalGoalScreen" />
        <activity android:name=".homeScreen.addNormalGoalActivity" />
        <activity android:name=".homeScreen.addCustomGoalActivity" />
        <activity android:name=".LoginScreen.mainLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginScreen.RegisterFragments" />
        <activity android:name=".homeScreen.homeActivity" />
    </application>
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

</manifest>